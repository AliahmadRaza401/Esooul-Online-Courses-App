import 'package:flutter/widgets.dart';

class TopicModel {
  var id;
  var title;
  var image;
  var course;
  TopicModel(
      {@required this.id,
      @required this.title,
      @required this.image,
      @required this.course,
     });
}
